# 天氣查詢系統

前端：Vue 3 + Vite
後端：FastAPI + SQLModel + SQLite
部署：Render

## 開發:

```bash
# 前端
cd frontend
npm install
npm run dev

# 後端（另開終端）
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```
